terraform {
  required_providers {
    aws = {
        source = "hashicorp/aws"
        value = "value"
    }
  }

# Provider
}
provider "aws" {
    region = ""
  access_key = ""
  secret_key = ""
}
